# 传送书签 0.1.2：Windows UI 快速复核

`0.1.1` 的记录、重命名、收藏、传送、删除、隐藏和恢复已经通过，本轮不重复完整测试。

构建并覆盖安装：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\Build-Windows.ps1 -Install
```

载入原来的单人存档后只检查：

1. 按 K 打开手册，窗口外应保留原游戏画面，不再出现蓝色山景菜单背景。
2. 手册主体应为统一的原版羊皮纸颜色，没有额外的详情区深浅色块。
3. 右侧按钮分为两排：下排“传送/收藏”，上排“重命名/删除或隐藏”；所有按钮都应在羊皮纸外框内。
4. 自定义书签二次确认显示“确认删除”，默认地点二次确认显示“确认隐藏”。

如方便，只需发一张打开手册的截图；不必再次提供完整操作日志，除非出现红色报错。
