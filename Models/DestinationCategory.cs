namespace WarpBookmarks.Models;

/// <summary>The main travel-book views. This is session UI state, not saved player data.</summary>
internal enum DestinationCategory
{
    Common,
    All,
    Bookmarks,
    Defaults,
    Hidden
}
