# Changelog

## 1.0.1 — 2026-09-10

- 将五个分类移到横跨手册的完整分类栏，避免英文标签互相重叠；
- 右侧详情与地点列表顶部对齐，并为长空状态、字段值和动态按钮增加宽度约束；
- 详情标签与标点改为本地化格式，英文使用半角冒号，避免缺字乱码；
- 英文界面按需使用游戏中文界面的 `SmallFont` 显示已有中文书签名和中英混合文本；
- 调整中文回退字形的字号和垂直对齐，避免列表、标题与混排按钮中的中文过大或下沉；
- 常用标记改为每行右侧固定位置的小型金色像素星星，不再挤压名称，也不会在英文界面被替换为问号；
- 英文分类 `Common` 改为更明确的 `Quick Access`，操作改为 `Pin / Unpin`；中文继续使用“常用 / 加入常用 / 移出常用”；
- 搜索提示移入输入框内部，给标题和搜索框留出稳定间距；
- 精准坐标的下拉箭头改为字体无关的像素三角形，避免 `▲/▼` 在部分语言字体中显示异常；
- 中文主传送按钮移除字体不支持的 `「」`，不再在地点名前后显示问号；
- 主列表滚动条按实际六行内容高度收短 28 个逻辑像素，与最后一行底边对齐；
- 中文分类“我的书签”精简为“书签”，避开该字形组合的异常视觉间距，不改变其他分类字距；
- 扩宽精准坐标窗口的三个底部操作按钮，并限制按钮文案不越过边框；
- 已保存的地图显示名在打开手册时按当前游戏语言刷新，不修改原始书签数据；
- 修复 Windows PowerShell 5.1 打包时未显式加载 `System.IO.Compression` 而找不到 `ZipArchive` 的问题，并让工程版本与 `manifest.json` 的 `1.0.1` 保持一致。

## 1.0.0 — 2026-08-06

首个公开版本。

- 使用可配置的 `K` 打开传送手册，使用 `Shift + K` 快速记录当前位置；
- 支持个人书签创建、命名、搜索、收藏、重命名和删除；
- 支持返回住宅门外、上一个位置和五个原版图腾地点；
- 支持可搜索地图列表和 X/Y 格子坐标传送；
- 支持默认地点隐藏、单独恢复和全部恢复；
- 所有入口共用事件、特殊地图、地图边界和三格安全落点检查；
- 单人和在线联机使用独立的每存档、每玩家书签数据；
- 提供简体中文和英文界面。

已知限制：重命名书签时左右方向键不能移动光标；可以使用退格后重新输入，不影响保存和传送。
